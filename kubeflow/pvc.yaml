apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fraud-artifacts-pvc
  namespace: fraud-detection
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: standard  # Use your cluster's storage class
  resources:
    requests:
      storage: 10Gi            # 10GB for models + data artifacts
